import styled from '@emotion/styled'

export const ModalContainer = styled.button`
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  max-width: 500px;
  padding: 40px;
  border-radius: 10%;
`